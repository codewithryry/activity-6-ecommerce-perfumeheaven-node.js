<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.name %> - Perfume Heaven</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        /* Global Styles */
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f8f8f8;
            display: flex;
            flex-direction: column;
            min-height: 100vh; /* Make sure the body takes the full height */
        }

        /* Navigation Bar */
        .top-bar {
            background-color: #333;
            color: #fff;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 0;
        }

        .top-bar .title-area h1 a {
            color: #fff;
            text-decoration: none;
            padding-left: 20px;
            font-size: 1.8em;
        }

        .top-bar .title-area h1 {
            margin: 0;
        }

        .top-bar .right {
            list-style: none;
            margin: 0;
            padding-right: 20px;
            display: flex;
            align-items: center;
        }

        .top-bar li {
            padding: 0 10px;
        }

        .top-bar a {
            color: #fff;
            text-decoration: none;
            font-size: 1.1em;
        }

        .top-bar a:hover {
            color: #ddd;
        }

        .top-bar .right .active a {
            text-decoration: underline;
            color: #ddd;
        }

        /* Main Content */
        .container {
            padding: 20px;
            flex: 1; /* Allow the container to grow and take available space */
            max-width: 800px; /* Limit max width for better readability */
            margin: 0 auto; /* Center the container */
            background-color: #fff; /* White background for product display */
            border-radius: 5px; /* Rounded corners */
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1); /* Subtle shadow */
        }

        .container h1 {
            font-size: 2em;
            margin-bottom: 15px;
            color: #333;
        }

        .container img {
            display: block; /* Center image */
            margin: 0 auto 15px; /* Center image and add margin */
            width: 300px; /* Fixed width */
            height: auto; /* Maintain aspect ratio */
            border-radius: 5px; /* Rounded corners for image */
        }

        .container p {
            margin: 0;
            margin-bottom: 15px;
            color: #666; /* Gray color for text */
        }

        .add-to-cart {
            display: flex;
            justify-content: center; /* Center the button */
            margin: 20px 0; /* Add some vertical spacing */
        }

        button {
            background-color: #28a745;
            color: #fff;
            padding: 8px 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1em;
        }

        button:hover {
            background-color: #218838;
        }

        /* Footer */
        footer {
            background-color: #333; /* Ensure footer background matches */
            color: #fff; /* Ensure footer text color matches */
            text-align: center;
            font-size: 0.9em; /* Consistent font size */
            padding: 20px; /* Consistent padding */
            margin-top: auto; /* Push footer to the bottom */
        }

        footer p {
            margin: 0;
        }
    </style>
</head>
<body>
    <nav class="top-bar">
        <div class="title-area">
            <h1><a href="/">Perfume Heaven</a></h1>
        </div>
        <section class="top-bar-section">
            <ul class="right">
                <li><a href="/about">About</a></li>
                <li><a href="/products">Perfume Collection</a></li>
                <li><a href="/cart">View Cart</a></li>
                <li><a href="/orders">My Orders</a></li>
                <li><a href="/contact">Contact</a></li>
                <% if (user) { %>
                    <li><a href="/profile">My Account</a></li>
                    <li><a href="/logout">Log Out</a></li>
                <% } else { %>
                    <li><a href="/login">Log In</a></li>
                    <li><a href="/register">Register</a></li>
                <% } %>
            </ul>
        </section>
    </nav>

    <div class="container">
        <h1><%= product.name %></h1>
        <% if (product.id === 15) { %>
            <img src="/uploads/1727582352027.png" alt="<%= product.name %>">
        <% } else if (product.id === 18) { %>
            <img src="/uploads/1727767449783.png" alt="<%= product.name %>">
        <% } else if (product.id === 17) { %>
            <img src="/uploads/1727583042101.png" alt="<%= product.name %>">
        <% } else { %>
            <p>No image available for this product.</p>
        <% } %>
    
        <p>Price: $<%= product.price %></p>
        <p>Stock: <%= product.stock %></p>
        <p>Description: <%= product.description %></p>
        <% if (user) { %>
            <div class="add-to-cart">
                <form action="/cart/add" method="POST">
                    <input type="hidden" name="productId" value="<%= product.id %>">
                    <button type="submit">Add to Cart</button>
                </form>
            </div>
        <% } else { %>
            <p><a href="/login">Log in</a> to add to cart</p>
        <% } %>
    </div>
    

    <footer>
        <p>&copy; 2024 Perfume Heaven. All Rights Reserved.</p>
    </footer>
</body>
</html>
